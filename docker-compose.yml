version: '3.8'

services:
  vidlink-extractor:
    build: .
    container_name: vidlink_stream_extractor
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
